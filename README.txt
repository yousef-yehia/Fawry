submission for software project 20200412_20200678_20200141_20200291

you should download and run the system as a maven project

We used eclipse EE for devolpement

the default username for admin is "admin "and the default password is '123'

the default username for a built in user is "user" and the default password is '123'

you should sign up if you want to add another user or you could log in using the present user

The default local host url is 'http://localhost:8080' 

we provided a table with every functionality for admin and user in our zipped submission containing every functionality and its method, URL and parameters
